// test/server/utils/helpers.test.js
// Add tests for helper functions
describe('helpers', () => {
  it('should have some tests', () => {
    expect(true).toBe(true);
  });

  it('should pass', () => {
    expect(1).toBe(1);
  });
});
